let ScrollPreserver = {
    mounted() {
        // Hook is kept for potential future use, but scroll preservation is removed
        // Year parameter is now handled entirely by LiveView
    },

    updated() {
        // No-op
    }
};

export default ScrollPreserver;