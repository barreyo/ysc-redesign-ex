import EctoEnum

defenum(EventState, ["draft", "published", "scheduled", "cancelled", "deleted"])
defenum(TicketStatus, ["pending", "confirmed", "cancelled"])
defenum(TicketTierType, ["free", "paid", "donation"])
